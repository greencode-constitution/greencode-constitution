Checkout the last upstream commit and do a final comparison. Then, commit the changes with the commit subject being what you wrote above, do separate commits per feature, then an empty commit with details as subject (what you wrote here). For that one, at the bottom add the final energy/speed results and the git hash of ../greencode-constitution that was used for the optimizations.